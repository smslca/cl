{
  "capital_inr": 50000,
  "risk_per_trade_pct": 1.0,
  "max_position_pct": 20,
  "_note": "EDIT THESE. capital_inr = trading capital. risk_per_trade_pct = max % of capital lost if a stop is hit. max_position_pct = cap on any single position's share of capital (tight stops otherwise create huge positions). Qty on the dashboard is computed from these."
}
